---
title: "Quickstart"
description: "Get started with our browsers"
---

<script>
  {(function(){
    function fixLinks(){
      var nodes=document.querySelectorAll('a[href^="/"]');
      nodes.forEach(function(a){
        var href=a.getAttribute('href')||'';
        if(!href) return;
        if(href.startsWith('/docs')) return;
        if(href.startsWith('/_next')||href.startsWith('/mintlify-assets')||href.startsWith('/logo')||href.startsWith('/socket.io')) return;
        a.setAttribute('href','/docs'+href);
      });
    }
    if(document.readyState!=='loading') fixLinks();
    else document.addEventListener('DOMContentLoaded',fixLinks);
  })()}
  </script>
## Quickconnect vs Session API:

| Feature | Quickconnect | Session API |
|---|---:|---:|
| One-line summary | Instant, zero-config browser access for quick tests and demos | Programmatic control of browser sessions for building workflows and automations |
| Setup & dependencies | Minimal — no SDK or server required; open a quick connection or URL | SDK or REST/websocket calls; integrate with your app/backend |
| Time to get started | Immediate (seconds) | Minutes — requires coding and auth setup |

## Choose how to get started

<CardGroup cols={2}>
  <Card title="Quickconnect" href="/docs/browser-api/using-quickconnect" icon="plug">
    Get started with our browsers immediately with zero configuration or dependencies using Quickconnect.
  </Card>
  <Card title="Session API" href="/docs/browser-api/using-session-api" icon="webhook">
    Build complex workflows using our Session API to manage browser sessions programmatically.
  </Card>
</CardGroup>
