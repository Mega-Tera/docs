
---
title: "Overview"
description: "Get started with our hosted browsers and browser agents."
---

<script defer src="/docs-prefill.js"></script>

<Tabs>
  <Tab title="Start Browser Session">
    <CodeGroup>

```bash cURL
curl -X POST "https://lisa-taurine.tera.space/v1/consumer/session" \
  -H "Authorization: Bearer $BROWSER_API_KEY" \
  -H "content-type: application/json" \
  -d '{}'
```

```python Python
import os
import requests

BROWSER_API_KEY = os.getenv("BROWSER_API_KEY")

resp = requests.post(
    "https://lisa-taurine.tera.space/v1/consumer/session",
    headers={
        "Authorization": f"Bearer {BROWSER_API_KEY}",
        "content-type": "application/json",
    },
    json={},
)
data = resp.json()
print("sessionId:", data.get("sessionId"))
```

```typescript TypeScript
const BROWSER_API_KEY = process.env.BROWSER_API_KEY!;

const res = await fetch("https://lisa-taurine.tera.space/v1/consumer/session", {
  method: "POST",
  headers: {
    Authorization: `Bearer ${BROWSER_API_KEY}`,
    "content-type": "application/json",
  },
  body: JSON.stringify({}),
});
const data = await res.json();
console.log("sessionId:", data.sessionId);
```

    </CodeGroup>

    <style>
      {`
        .run-status:empty { display: none }
        .run-response { display: none }
        .run-response:has(.run-output:not(:empty)) { display: block }
        /* Light = lighter surfaces + dark text; Dark = darker surfaces + light text */
        :root, [data-theme='light'] { --run-fg:#111111; --run-muted:#6b7280; --run-bg:#f7f7f7; --run-border:#e5e7eb }
        [data-theme='dark'] { --run-fg:#e5e7eb; --run-muted:#9ca3af; --run-bg:#1f2937; --run-border:#374151 }
        .run-btn{ color:var(--run-fg); background:var(--run-bg); border:1px solid var(--run-border); padding:6px 10px; border-radius:4px; cursor:pointer }
        .run-input{ color:var(--run-fg); background:transparent; border:1px solid var(--run-border); padding:6px 8px; border-radius:4px }
        .run-status{ color:var(--run-muted); font-size:12px; margin-left:10px }
        .run-output{ background:transparent; border:none; box-shadow:none }
        .run-icon{ position:absolute; top:8px; right:44px; width:26px; height:26px; display:flex; align-items:center; justify-content:center; border-radius:9999px; background:var(--run-bg); border:1px solid var(--run-border); color:var(--run-fg); cursor:pointer; z-index:2 }
        .run-inline{ width:24px; height:24px; display:inline-flex; align-items:center; justify-content:center; border-radius:9999px; background:var(--run-bg); border:1px solid var(--run-border); color:var(--run-fg); cursor:pointer; margin-right:8px }
      `}
    </style>
    <div className="run-request" style={{ marginTop: '12px', position: 'relative' }}>
      <button className="run-request-btn run-icon" data-url="https://lisa-taurine.tera.space/v1/consumer/session" data-method="POST" data-body="{}" aria-label="Run">▶</button>
      <span className="run-status"></span>
      <div className="run-response">
        <pre
          className="run-output"
          style={{ marginTop: '8px', maxHeight: '260px', overflow: 'auto' }}
        ></pre>
      </div>
    </div>

  </Tab>

  <Tab title="Trigger Agent Task">
    <CodeGroup>

```bash cURL
curl -X POST "https://tron.tera.space/v1/task/create" \
  -H "Authorization: Bearer $API_KEY" \
  -H "content-type: application/json" \
  -d '{"agent":"glitter","prompt":"What does example.com\'s homepage say?","mode":"text","stepLimit":10}'
```

```python Python
import os
import requests

API_BASE = os.getenv("API_BASE")
API_KEY = os.getenv("API_KEY")

resp = requests.post(
    API_BASE + "/v1/task/create",
    headers={
        "Authorization": f"Bearer {API_KEY}",
        "content-type": "application/json",
    },
    json={"agent": "glitter", "prompt": "What does example.com's homepage say?", "mode": "text", "stepLimit": 10},
)
print(resp.json())
```

```typescript TypeScript
const API_BASE = process.env.API_BASE!;
const API_KEY = process.env.API_KEY!;

const res = await fetch(API_BASE + "/v1/task/create", {
  method: "POST",
  headers: {
    Authorization: "Bearer " + API_KEY,
    "content-type": "application/json",
  },
  body: JSON.stringify({ agent: "glitter", prompt: "What does example.com's homepage say?", mode: "text", stepLimit: 10 }),
});
console.log(await res.json());
```

    </CodeGroup>

    <div className="run-request" style={{ marginTop: '12px', position: 'relative' }}>
      <button className="run-request-btn run-icon"
        data-url="https://tron.tera.space/v1/task/create"
        data-method="POST"
        data-body={`{"agent":"glitter","prompt":"What does example.com\u0027s homepage say?","mode":"text","stepLimit":10}`}
      >
        Run this request
      </button>
      <span className="run-status"></span>
      <div className="run-response">
        <pre className="run-output" style={{ marginTop: '8px', maxHeight: '260px', overflow: 'auto' }}></pre>
      </div>
    </div>

  </Tab>

  <Tab title="Get / Stop Session">
    <CodeGroup>

```bash cURL
# Get a session status
curl "https://lisa-taurine.tera.space/v1/consumer/session?sessionId=$SESSION_ID" \
  -H "Authorization: Bearer $BROWSER_API_KEY"

# Stop a session
curl -X DELETE "https://lisa-taurine.tera.space/v1/consumer/session?sessionId=$SESSION_ID" \
  -H "Authorization: Bearer $BROWSER_API_KEY"
```

```python Python
import os
import requests

BROWSER_API_KEY = os.getenv("BROWSER_API_KEY")
# Get a session status
SESSION_ID = os.getenv("SESSION_ID")
if SESSION_ID:
    status = requests.get(
        "https://lisa-taurine.tera.space/v1/consumer/session",
        headers={"Authorization": f"Bearer {BROWSER_API_KEY}"},
        params={"sessionId": SESSION_ID},
    )
    print(status.json())

    # Stop a session
    stop = requests.delete(
        "https://lisa-taurine.tera.space/v1/consumer/session",
        headers={"Authorization": f"Bearer {BROWSER_API_KEY}"},
        params={"sessionId": SESSION_ID},
    )
    print("stopped:", stop.status_code)
```

```typescript TypeScript
const BROWSER_API_KEY = process.env.BROWSER_API_KEY!;

// Get a session status
const SESSION_ID = process.env.SESSION_ID!;
const status = await fetch(
  `https://lisa-taurine.tera.space/v1/consumer/session?sessionId=${encodeURIComponent(SESSION_ID)}`,
  { headers: { Authorization: `Bearer ${BROWSER_API_KEY}` } }
);
console.log(await status.json());

// Stop a session
const stop = await fetch(
  `https://lisa-taurine.tera.space/v1/consumer/session?sessionId=${encodeURIComponent(SESSION_ID)}`,
  {
    method: "DELETE",
    headers: { Authorization: `Bearer ${BROWSER_API_KEY}` },
  }
);
console.log("stopped", stop.status);
```

    </CodeGroup>

    <div style={{ marginTop: '12px' }}>
      <input id="session-id-input" className="run-input" placeholder="sessionId" style={{ width: '260px' }} />
    </div>
    <div className="run-request" style={{ marginTop: '8px', position: 'relative' }}>
      <button className="run-request-btn run-icon"
        data-url="https://lisa-taurine.tera.space/v1/consumer/session?sessionId={sessionId}"
        data-method="GET"
        data-vars='{"sessionId":"#session-id-input"}'
        style={{ background: '#222', color: '#fff', border: '1px solid #444', padding: '6px 10px', borderRadius: '4px', cursor: 'pointer' }}>
        Get session
      </button>
      <span className="run-status"></span>
      <div className="run-response">
        <pre className="run-output" style={{ marginTop: '8px', maxHeight: '260px', overflow: 'auto' }}></pre>
      </div>
    </div>
    <div className="run-request" style={{ marginTop: '8px', position: 'relative' }}>
      <button className="run-request-btn run-icon"
        data-url="https://lisa-taurine.tera.space/v1/consumer/session?sessionId={sessionId}"
        data-method="DELETE"
        data-vars='{"sessionId":"#session-id-input"}'
        style={{ background: '#222', color: '#fff', border: '1px solid #444', padding: '6px 10px', borderRadius: '4px', cursor: 'pointer' }}>
        Stop session
      </button>
      <span className="run-status"></span>
      <div className="run-response">
        <pre className="run-output" style={{ marginTop: '8px', maxHeight: '260px', overflow: 'auto' }}></pre>
      </div>
    </div>

  </Tab>

  <Tab title="Get Agent Task">
    <CodeGroup>

```bash cURL
curl -H "Authorization: Bearer $API_KEY" \
  "https://tron.tera.space/v1/task/$TASK_ID"
```

```python Python
import os
import requests

API_BASE = os.getenv("API_BASE")
API_KEY = os.getenv("API_KEY")
TASK_ID = os.getenv("TASK_ID")

resp = requests.get(
    API_BASE + "/v1/task/" + str(TASK_ID),
    headers={"Authorization": f"Bearer {API_KEY}"},
)
print(resp.json())
```

```typescript TypeScript
const API_BASE = process.env.API_BASE!;
const API_KEY = process.env.API_KEY!;
const TASK_ID = process.env.TASK_ID!;

const res = await fetch(API_BASE + "/v1/task/" + TASK_ID, {
  headers: { Authorization: "Bearer " + API_KEY },
});
console.log(await res.json());
```

    </CodeGroup>

    <div style={{ marginTop: '12px' }}>
      <input id="task-id-input" className="run-input" placeholder="taskId" style={{ width: '260px' }} />
    </div>
    <div className="run-request" style={{ marginTop: '8px', position: 'relative' }}>
      <button className="run-request-btn run-btn"
        data-url="https://tron.tera.space/v1/task/{taskId}"
        data-method="GET"
        data-vars='{"taskId":"#task-id-input"}'
        style={{ background: '#222', color: '#fff', border: '1px solid #444', padding: '6px 10px', borderRadius: '4px', cursor: 'pointer' }}>
        Get task
      </button>
      <span className="run-status"></span>
      <div className="run-response">
        <pre className="run-output" style={{ marginTop: '8px', maxHeight: '260px', overflow: 'auto' }}></pre>
      </div>
    </div>

  </Tab>
</Tabs>

<h2 style={{ marginTop: '8px' }}>Products</h2>

<CardGroup cols={2}>
  <Card title="Browser API" href="/browser-api/quickstart" icon="browser">
    Launch hosted browsers for web automation,
    scraping, and testing at scale.
  </Card>
  <Card title="Agent API" href="/agent-api/quickstart" icon="robot">
    Deploy autonomous agents that can browse the web, interact with pages, and
    complete complex tasks.
  </Card>
</CardGroup>

